# Server-side only. Keep disabled until real provider values are configured.
EVALUATION_ENABLED=false
# OpenAI-compatible Chat Completions base URL (without /chat/completions).
EVALUATION_BASE_URL=https://your-provider.example/v1
EVALUATION_MODEL=
EVALUATION_API_KEY=
EVALUATION_TIMEOUT_MS=45000
